<ul class='booklist'>
  <% newBooks.forEach(book => { %>
    <li>
      <img src=<%= book.img %> alt=<%= book.desc %>>
      <h1> <%= book.title %></h1>
      <h3> <%= book.author %></h3>
      <a href="/books/<%= book.title %>">Details</a>
    </li>
  <% }) %>
</ul>